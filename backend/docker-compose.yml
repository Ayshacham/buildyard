services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
  redis-insight:
    image: redis/redisinsight:latest
    platform: linux/amd64
    ports:
      - "5540:5540"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis